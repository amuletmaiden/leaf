/* ==========================================================================
   MYSTERY VEIL
   Official change name: Mystery Veil

   Leaf may keep local state, but it never breaks immersion to explain itself.
   No banner, notice, panel, privacy command, erasure command, visitor ID,
   analytics payload, cookie, account, or server-side world save is created.
   ========================================================================== */
(function () {
  'use strict';

  globalThis.LEAF_HEARTH = {
    officialName: 'Mystery Veil',
    show() {},
    hide() {},
    isOpen() { return false; }
  };
})();
